<!DOCTYPE html>
<html lang="fr">
<head>
<link rel="icon" type="image/png" href="/images/logo.png">
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SpoonGram</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
.navbar, .nav, .nav-links, .nav a, nav, header nav {
  font-size: 0.92em; /* ou essaye 0.9em ou 14px selon ton goût */
}
.hashtag {
  color: #3897f0;
  text-decoration: none;
}
.hashtag:hover {
  text-decoration: underline;
}
.mention {
  color: #8e44ad;
  text-decoration: none;
}
.mention:hover {
  text-decoration: underline;
}
    body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; margin: 0; padding: 0; background: linear-gradient(135deg, #f3e7e9 0%, #e3eeff 100%); min-height: 100vh; }
    .container { max-width: 700px; margin: 0 auto; padding: 0 15px; }
    header { background: linear-gradient(90deg, #a8edea 0%, #fed6e3 100%); border-bottom: 1px solid #dbdbdb; padding: 18px 0 10px; position: sticky; top: 0; z-index: 100; box-shadow: 0 4px 16px rgba(140, 152, 164, 0.08);}
    header .container { display: flex; justify-content: space-between; align-items: center; }
    h1 { margin: 0; font-size: 28px; color: #333; letter-spacing: 1px; display: flex; align-items: center;}
    h1 a { color: inherit; text-decoration: none; display: flex; align-items: center;}
    h1 i { color: #ff6b6b; margin-right: 10px; }
    nav a { margin-left: 15px; color: #333; font-size: 22px; text-decoration: none; transition: transform 0.2s; }
    nav a:hover { transform: scale(1.15); color: #ff6b6b;}
    .alert { padding: 10px; margin: 10px 0; border-radius: 4px; }
    .success { background-color: #d4edda; color: #155724; }
    .error { background-color: #f8d7da; color: #721c24; }
    .card { background-color: white; border: 1px solid #dbdbdb; border-radius: 18px; margin-bottom: 32px; box-shadow: 0 8px 32px rgba(168, 237, 234, 0.08); overflow: hidden; }
    .post-header { display: flex; align-items: center; padding: 16px 20px; background: #fafafa;}
    .avatar { width: 40px; height: 40px; border-radius: 50%; margin-right: 12px; box-shadow: 0 2px 8px #e3eeff;}
    .username { font-weight: bold; margin-right: 10px; font-size: 1.07em;}
    .location { font-size: 13px; color: #8e8e8e; }
    .location i { margin-right: 3px; }
    .post-image { width: 100%; display: block; max-height: 320px; object-fit: cover; }
    .post-actions { padding: 12px 20px; display: flex; align-items: center;}
    .like-btn, .comment-btn, .btn { background: linear-gradient(90deg, #a8edea 0%, #fed6e3 100%); border: none; font-size: 18px; margin-right: 14px; cursor: pointer; color: #ff6b6b; border-radius: 50%; width: 38px; height: 38px; display: flex; align-items: center; justify-content: center; transition: box-shadow 0.2s, transform 0.2s, background 0.2s; box-shadow: 0 2px 8px #e3eeff;}
    .btn-edit { color: #1976d2; background: #e3eeff;}
    .btn-delete { color: #e74c3c; background: #f8d7da;}
    .btn:hover { transform: scale(1.12);}
    .post-caption { padding: 0 20px 18px; font-size: 1.03em; }
    .comments { padding: 0 20px 18px; }
    .comment { margin-bottom: 10px; }
    .comment-user { font-weight: bold; }
    .comment-form { display: flex; padding: 0 20px 18px;}
    .comment-form input { flex: 1; padding: 8px; border: 1px solid #dbdbdb; border-radius: 4px; }
    .comment-form button { margin-left: 10px; padding: 0 15px; background-color: #3897f0; color: white; border: none; border-radius: 4px; cursor: pointer; }
    .profile-header { display: flex; align-items: center; padding: 20px 0; }
    .profile-avatar { width: 80px; height: 80px; border-radius: 50%; margin-right: 20px; }
    .profile-info h2 { margin: 0; }
    .profile-stats { display: flex; margin-top: 10px; }
    .profile-stat { margin-right: 20px; }
    .profile-posts { display: grid; grid-template-columns: repeat(3, 1fr); gap: 5px; margin-top: 20px; }
    .profile-post { aspect-ratio: 1; overflow: hidden; position:relative;}
    .profile-post img { width: 100%; height: 100%; object-fit: cover; }
    .profile-post-actions { position:absolute; top:5px; right:5px;}
    .auth-form { background-color: white; border: 1px solid #dbdbdb; border-radius: 8px; padding: 20px; text-align: center; }
    .auth-form h2 { margin-top: 0; }
    .auth-form input { display: block; width: 100%; margin-bottom: 15px; padding: 10px; border: 1px solid #dbdbdb; border-radius: 4px; }
    .auth-form button { width: 100%; padding: 10px; background-color: #3897f0; color: white; border: none; border-radius: 4px; font-weight: bold; cursor: pointer; }
    .map-container { background: white; padding: 20px; border-radius: 8px; border: 1px solid #dbdbdb; }
    @media (max-width: 700px) {
      .container { max-width: 98vw; }
      .profile-posts { grid-template-columns: repeat(2, 1fr);}

    }
.post-image-fullwidth {
  width: 100%;
  height: auto;
  display: block;
  margin: 0;
  object-fit: cover;
  border-radius: 8px;
  min-height: 300px;
}

    @media (max-width: 530px) {
      .post-header, .post-actions, .post-caption, .comments, .comment-form { padding-left: 8px; padding-right: 8px;}
      .upload-form { padding: 14px 5px;}
      .profile-posts { grid-template-columns: 1fr;}
    }
  </style>
</head>
<body>
<header>
  <div class="container">
    <h1><a href="/"><i class="fas fa-utensil-spoon"></i> SpoonGram</a></h1>
    <nav>
      <a href="/"><i class="fas fa-home"></i></a>
      <a href="/map"><i class="fas fa-map-marked-alt"></i></a>
      <% if (currentUser) { %>
        <a href="/albums"><i class="fas fa-book"></i> </a>
        <a href="/feed"><i class="fas fa-users"></i> </a>
       <a href="/profile"><i class="fas fa-user"></i> </a>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i></a>
      <% } else { %>
        <a href="/login"><i class="fas fa-sign-in-alt"></i></a>
        <a href="/register"><i class="fas fa-user-plus"></i></a>
      <% } %>
    </nav>
  </div>
</header>
